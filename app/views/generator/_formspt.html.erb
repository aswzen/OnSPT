  <%= form_tag("/search", :method => "get") do %>
  <div class="row">           
      <div class="col-md-4 col-md-offset-4">

          <!-- alert notification -->
           

          <!-- end: alert notification -->

          <h3 class="form-title"><%= @judulform %></h3>

          <form role="form">
              <div class="form-group">
                  <label for="id_surat">Pilih Tugas</label>
                  <%= collection_select(:lang_id, 0, Spt.all, :id, :id)%> 
              </div>
        
              <div class="form-group">
                  <label for="id_user">Pilih Karyawan</label>               
                
<%= collection_select(:user, :id, User.all, :id, :nama_depan) %>
              </div>
              <div class="form-group">
                 
              </div>                                                             
              <%= submit_tag("Simpan") %>
          </form>

      </div><!-- end: col-md-4 col-md-offset-4 -->
  </div><!-- end: .row -->
<% end %>
